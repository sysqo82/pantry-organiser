# Automatic Shopping List Enrollment

To ensure the pantry never runs out of basic necessities without manual overhead, the system automatically adds items to the **Shopping List** when they enter a **Low Stock** state or are **Deleted** (consumed to zero). This "opt-out" approach ensures that inventory management leads directly to procurement without requiring the user to manually flag items after every consumption event.
